import { Skeleton } from "@mui/material"
import { ISkeleton, SkeletonNumbers } from "../../types/skeleton.type";

export const LoadingSkeleton = ({ variant, width, height }: ISkeleton) => {
    return <Skeleton
    // variant={variant}
    // width={width}
    // height={height}
    >
    </Skeleton>
}